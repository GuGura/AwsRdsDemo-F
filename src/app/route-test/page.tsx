import Button from "@/app/_components/Button";

export default async function Page() {
  return (
    <div style={{height:200, flex:1 , display:'flex' , justifyContent:'center' }}>
      <Button/>
    </div>
  );
}
